// Code generation driver for the brainpoolP256r1 SCALAR field arithmetic, i.e.
// integers modulo the group order n (RFC 5639 §3.4). Mirrors the field-prime
// generator in ../fiat, using the same pinned fiat-crypto:v0.0.9 image and
// flags. See ../fiat/PROVENANCE.md for how to build the image.
//
// Run with: go run generate.go

//go:build ignore

package main

import (
	"bytes"
	"go/format"
	"log"
	"os"
	"os/exec"
)

// brainpoolP256r1 group order n (RFC 5639 §3.4). Hex for fiat-crypto, decimal
// for addchain.
const (
	order    = "0xA9FB57DBA1EEA9BC3E660A909D838D718C397AA3B561A6F7901E0E82974856A7"
	orderDec = "76884956397045344220809746629001649092737531784414529538755519063063536359079"
)

func main() {
	log.Println("Generating bp256n_fiat64.go (fiat-crypto:v0.0.9)...")
	cmd := exec.Command("docker", "run", "--rm", "--platform", "linux/amd64",
		"--entrypoint", "word_by_word_montgomery", "fiat-crypto:v0.0.9",
		"--lang", "Go", "--no-wide-int", "--cmovznz-by-mul",
		"--relax-primitive-carry-to-bitwidth", "32,64", "--internal-static",
		"--public-function-case", "camelCase", "--public-type-case", "camelCase",
		"--private-function-case", "camelCase", "--private-type-case", "camelCase",
		"--doc-text-before-function-name", "", "--doc-newline-before-package-declaration",
		"--doc-prepend-header", "Code generated by Fiat Cryptography. DO NOT EDIT.",
		"--package-name", "fiatn", "--no-prefix-fiat", "bp256n", "64", order,
		"mul", "square", "add", "sub", "one", "from_montgomery", "to_montgomery",
		"selectznz", "to_bytes", "from_bytes")
	cmd.Stderr = os.Stderr
	out, err := cmd.Output()
	if err != nil {
		log.Fatal(err)
	}
	if out, err = format.Source(out); err != nil {
		log.Fatal(err)
	}
	if err := os.WriteFile("bp256n_fiat64.go", out, 0644); err != nil {
		log.Fatal(err)
	}

	log.Println("Generating bp256n_invert.go (addchain)...")
	search := exec.Command("addchain", "search", orderDec+" - 2")
	search.Stderr = os.Stderr
	chain, err := search.Output()
	if err != nil {
		log.Fatal(err)
	}
	chainFile, err := os.CreateTemp("", "bp256n-addchain")
	if err != nil {
		log.Fatal(err)
	}
	defer os.Remove(chainFile.Name())
	if _, err := chainFile.Write(chain); err != nil {
		log.Fatal(err)
	}
	if err := chainFile.Close(); err != nil {
		log.Fatal(err)
	}
	tmplFile, err := os.CreateTemp("", "bp256n-invert-tmpl")
	if err != nil {
		log.Fatal(err)
	}
	defer os.Remove(tmplFile.Name())
	if err := os.WriteFile(tmplFile.Name(), []byte(invertTmpl), 0644); err != nil {
		log.Fatal(err)
	}
	gen := exec.Command("addchain", "gen", "-tmpl", tmplFile.Name(), chainFile.Name())
	gen.Stderr = os.Stderr
	out, err = gen.Output()
	if err != nil {
		log.Fatal(err)
	}
	out = bytes.ReplaceAll(out, []byte("Element"), []byte("Element"))
	if out, err = format.Source(out); err != nil {
		log.Fatal(err)
	}
	if err := os.WriteFile("bp256n_invert.go", out, 0644); err != nil {
		log.Fatal(err)
	}
	log.Println("Done.")
}

const invertTmpl = `// Code generated by {{ .Meta.Name }}. DO NOT EDIT.

package fiatn

// Invert sets e = 1/x mod n, and returns e.
//
// If x == 0, Invert returns e = 0.
func (e *Element) Invert(x *Element) *Element {
	// Inversion is implemented as exponentiation with exponent n − 2.
	// The sequence of {{ .Ops.Adds }} multiplications and {{ .Ops.Doubles }} squarings is derived from the
	// following addition chain generated with {{ .Meta.Module }} {{ .Meta.ReleaseTag }}.
	//
	{{- range lines (format .Script) }}
	//	{{ . }}
	{{- end }}
	//

	var z = new(Element).Set(e)
	{{- range .Program.Temporaries }}
	var {{ . }} = new(Element)
	{{- end }}
	{{ range $i := .Program.Instructions -}}
	{{- with add $i.Op }}
	{{ $i.Output }}.Mul({{ .X }}, {{ .Y }})
	{{- end -}}

	{{- with double $i.Op }}
	{{ $i.Output }}.Square({{ .X }})
	{{- end -}}

	{{- with shift $i.Op -}}
	{{- $first := 0 -}}
	{{- if ne $i.Output.Identifier .X.Identifier }}
	{{ $i.Output }}.Square({{ .X }})
	{{- $first = 1 -}}
	{{- end }}
	for s := {{ $first }}; s < {{ .S }}; s++ {
		{{ $i.Output }}.Square({{ $i.Output }})
	}
	{{- end -}}
	{{- end }}

	return e.Set(z)
}
`
